export interface IPaths {
  pathToHomePage: string;
  pathToCarsPage: string;
  pathToDealersPage: string;
}

const paths: IPaths = {
  pathToHomePage: '/',
  pathToCarsPage: '/cars',
  pathToDealersPage: '/dealers',
};

export default paths;
